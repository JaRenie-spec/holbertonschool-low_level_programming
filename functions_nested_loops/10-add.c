#include "main.h"
#include <stdio.h>

/**
 *add - function that adds two integers and returns the result
 *@a: integer a
 *@b: integer b
 * Return: 0 success
 */
int add(int a, int b)
{
	return (a + b);
}
